# DJD 차트생성 도우미 - 완전한 개발 문서

> **프로젝트명**: djd_soap_rece00
> **작성일**: 2025-12-09
> **버전**: 1.0.0
> **개발 도구**: React + TypeScript + Vite + Tailwind CSS

---

## 📋 목차

1. [프로젝트 개요](#1-프로젝트-개요)
2. [사업 계획 및 목적](#2-사업-계획-및-목적)
3. [기술 스택](#3-기술-스택)
4. [시스템 아키텍처](#4-시스템-아키텍처)
5. [주요 기능 상세](#5-주요-기능-상세)
6. [파일 구조 및 설명](#6-파일-구조-및-설명)
7. [코드 상세 분석](#7-코드-상세-분석)
8. [개발 히스토리](#8-개발-히스토리)
9. [설치 및 실행 가이드](#9-설치-및-실행-가이드)
10. [API 및 외부 서비스 연동](#10-api-및-외부-서비스-연동)
11. [보안 및 권한 관리](#11-보안-및-권한-관리)
12. [향후 개선 계획](#12-향후-개선-계획)
13. [문제 해결 가이드](#13-문제-해결-가이드)
14. [유지보수 가이드](#14-유지보수-가이드)

---

## 1. 프로젝트 개요

### 1.1 프로젝트 설명
**DJD 차트생성 도우미**는 대한민국 한의원에서 사용하는 실시간 진료 기록 자동화 시스템입니다. 의사와 환자의 대화를 실시간으로 녹음하고, Google Gemini AI를 사용하여 음성을 텍스트로 전사한 후, 표준화된 SOAP(Subjective, Objective, Assessment, Plan) 형식의 진료 차트를 자동으로 생성합니다.

### 1.2 핵심 가치
- ⏱️ **시간 절약**: 진료 후 차트 작성 시간을 80% 이상 단축
- ✅ **정확성 향상**: AI 기반 전사 및 검수로 오탈자 최소화
- 📊 **표준화**: 모든 진료 기록을 SOAP 형식으로 통일
- 🔄 **실시간 처리**: 진료 중 실시간으로 대화 내용 캡처
- 🔒 **보안**: 로컬 처리 및 Google Drive 연동으로 안전한 데이터 관리

### 1.3 주요 통계
- **총 코드 라인 수**: 1,911 줄
  - App.tsx: 1,501 줄
  - geminiService.ts: 241 줄
  - icons.tsx: 169 줄
- **총 커밋 수**: 14개
- **개발 기간**: 2025년 11월 30일 ~ 2025년 12월 9일 (9일)
- **지원 언어**: 한국어
- **사용 AI 모델**: Gemini 2.5 Flash, Gemini 3 Pro Preview

---

## 2. 사업 계획 및 목적

### 2.1 사업 배경
한의원 진료 환경에서는 의사가 환자와의 대화를 통해 증상을 파악하고 진단하지만, 진료 후 기록 작성에 많은 시간이 소요됩니다. 또한 수기 작성 시 오탈자나 누락이 발생할 수 있으며, 기록 형식이 의사마다 다를 수 있습니다.

### 2.2 목표 시장
- **1차 타겟**: 개인 한의원 (전국 약 15,000개)
- **2차 타겟**: 중소형 한방병원
- **3차 타겟**: 한의학 관련 교육기관

### 2.3 제공 가치
1. **진료 효율성 극대화**
   - 진료 중 실시간 녹음 및 전사
   - 진료 직후 즉시 차트 생성
   - 의사는 환자에게만 집중 가능

2. **법적 리스크 감소**
   - 모든 진료 내용 정확히 기록
   - 표준화된 형식으로 보험청구 간소화
   - 의료분쟁 시 증거자료 확보

3. **데이터 기반 진료 개선**
   - 진료 패턴 분석 가능
   - 자주 사용하는 치료법 통계 확보
   - 환자 증상별 효과적인 치료법 도출

### 2.4 수익 모델 (향후 계획)
- **SaaS 구독**: 월 ₩50,000 ~ ₩150,000
- **API 사용료**: 초과 사용 시 종량제
- **커스터마이징**: 병원별 맞춤 개발
- **교육 및 컨설팅**: 도입 지원 서비스

### 2.5 차별화 요소
- ✅ 한의학 특화 (경혈명, 한약재명 정확 인식)
- ✅ 실시간 2분 단위 청킹으로 긴 진료 지원
- ✅ AI 환각(Hallucination) 자동 검수 기능
- ✅ Google Drive 자동 저장으로 백업 자동화
- ✅ 오프라인에서도 녹음 가능 (추후 동기화)

---

## 3. 기술 스택

### 3.1 프론트엔드
| 기술 | 버전 | 용도 |
|------|------|------|
| **React** | 19.2.0 | UI 프레임워크 |
| **TypeScript** | 5.2.2 | 정적 타입 검사 |
| **Vite** | 5.2.0 | 빌드 도구 및 개발 서버 |
| **Tailwind CSS** | 3.4.1 | UI 스타일링 |
| **PostCSS** | 8.4.35 | CSS 전처리 |

### 3.2 AI 및 백엔드 서비스
| 서비스 | 용도 |
|--------|------|
| **Google Gemini 2.5 Flash** | 실시간 음성 전사 (저지연) |
| **Google Gemini 3 Pro Preview** | SOAP 차트 생성 및 전사 검수 |
| **Google Calendar API** | 일정 확인 |
| **Google Tasks API** | 할 일 관리 |
| **Google Drive API** | 차트 자동 저장 |

### 3.3 개발 도구
- **Git/GitHub**: 버전 관리
- **npm**: 패키지 관리
- **ESLint**: 코드 품질 관리
- **TypeScript Compiler**: 타입 체크

### 3.4 브라우저 API
- **MediaRecorder API**: 마이크 녹음
- **FileReader API**: 파일 처리
- **Clipboard API**: 텍스트 복사
- **Drag & Drop API**: 파일 드래그 앤 드롭

---

## 4. 시스템 아키텍처

### 4.1 전체 시스템 구조도

```
┌─────────────────────────────────────────────────────────────┐
│                        사용자 (의사)                          │
└───────────────────────┬─────────────────────────────────────┘
                        │
                        ▼
┌─────────────────────────────────────────────────────────────┐
│                  웹 브라우저 (React App)                      │
│                                                               │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐         │
│  │  음성 녹음   │  │ 실시간 전사  │  │ 차트 생성    │         │
│  │  (2분 청킹)  │  │  + 검수     │  │ (SOAP)      │         │
│  └─────────────┘  └─────────────┘  └─────────────┘         │
│                                                               │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐         │
│  │ Google 연동  │  │ 로컬 저장    │  │ Drive 저장   │         │
│  │ (Calendar/  │  │ (Download)  │  │ (자동 백업)  │         │
│  │  Tasks)     │  │             │  │             │         │
│  └─────────────┘  └─────────────┘  └─────────────┘         │
└───────────────────────┬─────────────────────────────────────┘
                        │
        ┌───────────────┼───────────────┐
        ▼               ▼               ▼
┌──────────────┐ ┌──────────────┐ ┌──────────────┐
│ Gemini 2.5   │ │ Gemini 3 Pro │ │ Google APIs  │
│ Flash        │ │ Preview      │ │ (Calendar/   │
│ (음성 전사)   │ │ (차트 생성/  │ │  Tasks/      │
│              │ │  검수)       │ │  Drive)      │
└──────────────┘ └──────────────┘ └──────────────┘
```

### 4.2 데이터 흐름

```
[진료 시작]
    │
    ▼
[녹음 시작] → [2분마다 자동 청킹]
    │              │
    │              ▼
    │         [Blob 저장]
    │              │
    ▼              │
[녹음 중지] ────────┤
    │              │
    ▼              ▼
[모든 Blob 수집] → [순차적 전사 (Gemini 2.5 Flash)]
    │
    ▼
[전사 결과 통합] → [전사 검수 (Gemini 3 Pro)]
    │
    ▼
[검수된 전사문 + 추가 메모] → [SOAP 차트 생성 (Gemini 3 Pro)]
    │
    ▼
[차트 완성] → [환자명 추출] → [파일명 생성]
    │                              │
    ├──────────────────────────────┤
    │                              │
    ▼                              ▼
[로컬 저장 (txt)]            [Google Drive 저장]
```

### 4.3 주요 컴포넌트 관계도

```
App.tsx (Main Component)
│
├─ SettingsModal
│   └─ API 키 설정 (Gemini, Google)
│
├─ CalendarModal
│   └─ Google Calendar 일정 표시
│
├─ TasksModal
│   └─ Google Tasks 관리
│
└─ 메인 UI
    ├─ 녹음 컨트롤 (Record/Stop)
    ├─ 전사 내용 패널 (편집 가능)
    ├─ 추가 메모 패널 (파일 첨부 가능)
    └─ SOAP 차트 패널 (편집 가능)
```

---

## 5. 주요 기능 상세

### 5.1 실시간 음성 녹음 (2분 청킹)

**목적**: 긴 진료 시간에도 안정적으로 녹음하고 전사

**동작 원리**:
1. 녹음 시작 시 `MediaRecorder` API 사용
2. `setInterval`로 2분(120초)마다 자동으로 녹음 중지
3. 중지된 오디오를 `Blob`으로 저장
4. 즉시 새 녹음 재시작 (끊김 없이)
5. 사용자가 "중지" 버튼 누를 때까지 반복

**코드 위치**: `App.tsx` 라인 754-826

**장점**:
- 메모리 오버플로우 방지
- 네트워크 타임아웃 방지
- 긴 진료에도 안정적 처리

### 5.2 AI 기반 음성 전사

**사용 모델**: Gemini 2.5 Flash (Audio-native, 저지연)

**전사 프로세스**:
1. 각 오디오 Blob을 Base64로 인코딩
2. 이전 전사 내용을 문맥(context)으로 제공
3. Gemini에 전송하여 한국어 전사
4. 전사 결과를 순차적으로 연결

**주요 프롬프트 규칙**:
- 실제로 들리는 내용만 전사
- 추측하거나 없는 내용 추가 금지
- 잘 들리지 않는 부분은 `[불명확]` 표시
- 의학 용어 정확히 받아 적기
- 요약 없이 전사 텍스트만 출력

**코드 위치**: `services/geminiService.ts` 라인 18-51

### 5.3 전사 내용 자동 검수 (환각 제거)

**목적**: AI 음성 인식 오류 및 환각(hallucination) 자동 수정

**검수 AI**: Gemini 3 Pro Preview

**검수 항목**:
1. **오탈자 및 문법 오류** 교정
2. **의학 용어** 정확성 확인 (경혈명, 약재명, 병증명)
3. **환각 탐지 및 제거**:
   - 같은 내용 반복
   - 문맥 무관한 단어/문장
   - 배경 소음으로 인식된 텍스트
4. **불필요한 중복** 제거
5. **진료 무관 내용** 제거

**코드 위치**: `services/geminiService.ts` 라인 54-111

### 5.4 SOAP 차트 자동 생성

**SOAP 형식**:
- **S (Subjective)**: 주호소, 현병력, 악화·완화 요인, 관련 증상
- **O (Objective)**: 시진, 촉진, ROM 검사, 활력징후
- **A (Assessment)**: 진단명, 의증
- **P (Plan)**: 시술, 치료 빈도, 한약, 예후, 주의사항, 생활지도

**생성 AI**: Gemini 3 Pro Preview

**핵심 원칙**:
- 기록에 있는 내용만 사용 (절대 추측 금지)
- 정보 없으면 "미확인" 표시
- 숫자, 경혈명, 용량 원문 그대로 유지
- 중요 수치는 굵은 글씨로 강조 (예: **5분**, **3장**)

**추가 기능**:
- 진료일시 자동 삽입 (KST 기준)
- 한줄 요약 자동 생성 (50자 내외)
- 확인사항 체크리스트 3개 항목

**코드 위치**: `services/geminiService.ts` 라인 114-241

### 5.5 Google 서비스 연동

#### 5.5.1 Google OAuth 2.0 인증
- 자동 토큰 갱신 (만료 5분 전)
- 로컬스토리지에 로그인 상태 저장
- 토큰 만료 시 자동 재인증

**코드 위치**: `App.tsx` 라인 479-612

#### 5.5.2 Google Calendar 연동
- 오늘 일정 자동 조회
- 일정별 색상 표시
- 시작/종료 시간 표시

**코드 위치**: `App.tsx` 라인 147-268

#### 5.5.3 Google Tasks 연동
- 할 일 조회/추가/수정/삭제
- 완료 상태 토글
- 실시간 동기화

**코드 위치**: `App.tsx` 라인 271-432

#### 5.5.4 Google Drive 자동 저장
- 지정 폴더(ID: `1XGJmZp53bm_o-zaDgEzMv36FIxEL2e1F`)에 저장
- 파일명 자동 생성: `SOAP차트_YYYYMMDD_HHMM_환자명.txt`
- Multipart 업로드 방식

**코드 위치**: `App.tsx` 라인 941-1027

### 5.6 외부 서비스 연동 버튼

**통합된 외부 링크**:
1. **DJD 진단**: https://djd-diagnosisv2.netlify.app/
2. **Google Calendar**: 모달로 오늘 일정 표시
3. **Google Tasks**: 모달로 할 일 관리
4. **Google Sheets**: https://docs.google.com/spreadsheets/
5. **Re-visit**: 병원 접수 관리 시스템
6. **doctalk 예약**: 온라인 예약 시스템
7. **Notion 위키**: 내부 지식 베이스

**코드 위치**: `App.tsx` 라인 1131-1206

---

## 6. 파일 구조 및 설명

```
/home/user/b/
│
├── .env.local                 # 환경 변수 (GEMINI_API_KEY)
├── .gitignore                 # Git 제외 파일 목록
├── README.md                  # 프로젝트 소개 및 실행 방법
├── metadata.json              # 프로젝트 메타데이터 (권한 설정)
│
├── package.json               # 의존성 패키지 목록
├── package-lock.json          # 의존성 잠금 파일
├── tsconfig.json              # TypeScript 설정
├── vite.config.ts             # Vite 빌드 설정
├── tailwind.config.js         # Tailwind CSS 설정
├── postcss.config.js          # PostCSS 설정
│
├── index.html                 # HTML 엔트리 포인트
├── index.tsx                  # React 엔트리 포인트
├── index.css                  # 전역 CSS (Tailwind 임포트)
├── App.tsx                    # 메인 컴포넌트 (1,501줄)
│
├── components/
│   └── icons.tsx              # SVG 아이콘 컴포넌트 (169줄)
│
└── services/
    └── geminiService.ts       # Gemini AI 통신 로직 (241줄)
```

### 6.1 주요 파일 상세 설명

#### `App.tsx` (1,501줄)
**역할**: 애플리케이션의 핵심 로직 및 UI

**주요 섹션**:
- **State 관리** (라인 438-466): 녹음, 전사, 차트, 설정 상태
- **Google API 초기화** (라인 498-558): OAuth 및 API 클라이언트 설정
- **토큰 자동 갱신** (라인 560-592): 5분 전 자동 갱신 로직
- **녹음 로직** (라인 754-855): 2분 청킹 녹음
- **전사 및 차트 생성** (라인 690-752): AI 처리 파이프라인
- **UI 컴포넌트** (라인 1139-1511): 모든 화면 요소

#### `services/geminiService.ts` (241줄)
**역할**: Gemini AI와의 모든 통신 처리

**주요 함수**:
1. `transcribeWithGemini()`: 음성 → 텍스트 전사
2. `verifyAndCorrectTranscript()`: 전사 내용 검수
3. `generateSoapChart()`: SOAP 차트 생성

**시스템 인스트럭션**:
- 전사용 프롬프트 (라인 22-31)
- 검수용 시스템 인스트럭션 (라인 54-70)
- 차트 생성용 시스템 인스트럭션 (라인 114-134)

#### `components/icons.tsx` (169줄)
**역할**: 모든 SVG 아이콘 컴포넌트 정의

**포함된 아이콘**:
- MicrophoneIcon, StopIcon, CopyIcon, SaveIcon
- SettingsIcon, GeminiIcon, CloseIcon, EditIcon, CheckIcon
- GoogleIcon, GoogleTasksIcon, GoogleSheetsIcon
- DjdLogoIcon, DoctalkIcon, NotionIcon, RevisitIcon
- AttachmentIcon, TrashIcon, PlusIcon, Spinner

#### `tailwind.config.js`
**커스텀 컬러**:
```javascript
'brand-primary': '#DC2626'     // 빨간색 (녹음 버튼)
'brand-secondary': '#B91C1C'   // 진한 빨간색
'brand-accent': '#FFC107'      // 노란색 (강조)
'brand-dark': '#1A202C'        // 다크 배경
'brand-google-blue': '#4285F4' // Google 블루
```

---

## 7. 코드 상세 분석

### 7.1 녹음 청킹 메커니즘

```typescript
// 2분마다 자동으로 녹음을 재시작하는 핵심 로직
chunkIntervalRef.current = setInterval(() => {
    if (mediaRecorderRef.current && mediaRecorderRef.current.state === 'recording') {
        isAutoRestartingRef.current = true;  // 자동 재시작 플래그
        mediaRecorderRef.current.stop();      // 현재 녹음 중지
    }
}, CHUNK_DURATION_MS);  // 120,000ms = 2분

mediaRecorderRef.current.onstop = async () => {
    const audioBlob = new Blob(audioChunksRef.current, { type: 'audio/webm' });
    if (audioBlob.size > 0) {
        completedAudioBlobsRef.current.push(audioBlob);  // 완료된 청크 저장
    }
    audioChunksRef.current = [];  // 버퍼 초기화

    if (isAutoRestartingRef.current) {
        // 자동 재시작이면 즉시 다시 녹음
        mediaRecorderRef.current?.start();
    } else {
        // 사용자가 중지했으면 전사 시작
        await processRecordedAudio(completedAudioBlobsRef.current, startTime, stream);
    }
};
```

**핵심 포인트**:
- `isAutoRestartingRef`: 자동 재시작 vs 사용자 중지 구분
- `completedAudioBlobsRef`: 완료된 오디오 청크 누적 저장
- `audioChunksRef`: 현재 녹음 중인 데이터 임시 저장

### 7.2 순차적 전사 처리

```typescript
for (let i = 0; i < blobs.length; i++) {
    setStatusMessage(`Gemini AI 음성 분석 중... (구간 ${i + 1}/${blobs.length})`);
    const prompt = fullTranscript.slice(-300);  // 이전 300자를 문맥으로 제공
    const chunkText = await transcribeWithGemini(geminiApiKey, blobs[i], prompt);
    fullTranscript += (fullTranscript ? ' ' : '') + chunkText;
}
```

**핵심 포인트**:
- 각 청크를 순차적으로 전사 (병렬 처리 X)
- 이전 전사 내용의 마지막 300자를 문맥으로 제공
- 전사 진행 상황을 UI에 표시

### 7.3 Google OAuth 토큰 자동 갱신

```typescript
useEffect(() => {
    if (!isGoogleSignedIn || !tokenClientRef.current) return;

    const checkAndRefreshToken = () => {
        const expiresAtStr = localStorage.getItem('googleTokenExpiresAt');
        if (!expiresAtStr) return;

        const expiresAt = parseInt(expiresAtStr, 10);
        const now = Date.now();
        const timeUntilExpiry = expiresAt - now;
        const REFRESH_THRESHOLD = 5 * 60 * 1000;  // 5분

        if (timeUntilExpiry < REFRESH_THRESHOLD && timeUntilExpiry > 0) {
            console.log('토큰이 곧 만료됩니다. 자동 갱신을 시도합니다.');
            tokenClientRef.current.requestAccessToken({ prompt: '' });
        } else if (timeUntilExpiry <= 0) {
            console.log('토큰이 만료되었습니다. 재인증이 필요합니다.');
            tokenClientRef.current.requestAccessToken({ prompt: 'none' });
        }
    };

    checkAndRefreshToken();  // 초기 체크
    const intervalId = setInterval(checkAndRefreshToken, 60 * 1000);  // 1분마다

    return () => clearInterval(intervalId);
}, [isGoogleSignedIn]);
```

**핵심 포인트**:
- 만료 5분 전에 자동 갱신 시도
- 1분마다 토큰 상태 확인
- localStorage에 만료 시간 저장

### 7.4 파일명 자동 생성

```typescript
const generateFilename = (prefix: string, extension: 'txt'): string => {
    const now = new Date();
    const timestamp = `${now.getFullYear()}${month}${day}_${hours}${minutes}`;

    // SOAP 차트에서 환자명 추출
    const match = soapChart.match(/환자명:\s*(.*)/);
    const patientNameRaw = match && match[1] ? match[1].trim() : '미확인';

    // 대괄호 제거 및 파일명 안전 문자로 변환
    let patientName = patientNameRaw.replace(/^\[(.*)\]$/, '$1').trim() || '미확인';
    patientName = patientName.replace(/[\\?%*:"|<>./]/g, '_');

    return `${prefix}_${timestamp}_${patientName}.${extension}`;
};
```

**출력 예시**: `SOAP차트_20251209_1430_홍길동.txt`

---

## 8. 개발 히스토리

### 8.1 타임라인

| 날짜 | 커밋 | 주요 변경사항 |
|------|------|--------------|
| 2025-11-30 | a275eac | 프로젝트 초기 설정 |
| 2025-12-01 | f6c5178 | 전사 환각(hallucination) 문제 해결 |
| 2025-12-02 | 6377334 | 로컬 리소스 관리 및 Google OAuth 토큰 자동 갱신 구현 |
| 2025-12-02 | 182472d | TypeScript 타입 오류 수정 및 Tailwind 설정 최적화 |
| 2025-12-02 | 946fe9b | DJD Diagnosis 버튼 추가 |
| 2025-12-02 | 1651a38 | Doctalk 예약 버튼 추가 |
| 2025-12-02 | 0f66390 | 배포 오류 수정: 파일 정리 및 TypeScript 이슈 해결 |
| 2025-12-02 | e5fd610 | 빌드 수정: React types 설치 및 TypeScript 오류 해결 |
| 2025-12-03 | e8327ff | 버튼 이름 변경: "닥톡예약" → "doctalk 예약" |
| 2025-12-03 | 2427f8e | PR #1 병합 |
| 2025-12-03 | d58dd9e | PR #2 병합 |
| 2025-12-03 | 5d7a2c5 | DJD Diagnosis 기능 병합 |
| 2025-12-03 | b65fe54 | 로컬 리소스 관리 및 OAuth 토큰 갱신 병합 |
| 2025-12-03 | 08ba197 | **PR #3 병합 - 모든 브랜치 통합 완료** |

### 8.2 주요 마일스톤

**Phase 1: 기본 기능 구현** (2025-11-30)
- ✅ 음성 녹음 기능
- ✅ Gemini AI 연동
- ✅ SOAP 차트 생성

**Phase 2: 품질 개선** (2025-12-01)
- ✅ 전사 환각 문제 해결
- ✅ 전사 검수 기능 추가

**Phase 3: 인프라 강화** (2025-12-02)
- ✅ Google OAuth 자동 갱신
- ✅ Tailwind CSS 도입
- ✅ TypeScript 타입 안정성 강화

**Phase 4: 기능 확장** (2025-12-02 ~ 2025-12-03)
- ✅ 외부 서비스 연동 버튼 추가
- ✅ Google Calendar/Tasks 연동
- ✅ Google Drive 자동 저장

**Phase 5: 통합 및 안정화** (2025-12-03)
- ✅ 모든 브랜치 병합
- ✅ 배포 준비 완료

### 8.3 해결한 주요 이슈

1. **전사 환각 문제** (f6c5178)
   - 문제: AI가 실제 없는 내용을 추가하거나 반복
   - 해결: Gemini 3 Pro로 검수 단계 추가, 환각 탐지 로직 구현

2. **긴 진료 녹음 시 타임아웃** (청킹 도입)
   - 문제: 10분 이상 녹음 시 네트워크 타임아웃 발생
   - 해결: 2분 단위로 청킹하여 순차 전사

3. **TypeScript 타입 오류** (182472d, e5fd610)
   - 문제: React 19 업데이트로 타입 불일치
   - 해결: @types/react@19.2.7, @types/react-dom@19.2.3 설치

4. **Google OAuth 토큰 만료** (6377334)
   - 문제: 1시간 후 토큰 만료로 API 호출 실패
   - 해결: 5분 전 자동 갱신 로직 구현

---

## 9. 설치 및 실행 가이드

### 9.1 사전 요구사항
- **Node.js**: v18 이상
- **npm**: v9 이상
- **Google Gemini API 키**: https://aistudio.google.com/app/apikey
- **Google Cloud 프로젝트**: Calendar, Tasks, Drive API 활성화

### 9.2 설치 단계

```bash
# 1. 저장소 클론
git clone https://github.com/choi0ne/b.git
cd b

# 2. 의존성 설치
npm install

# 3. 환경 변수 설정
echo "GEMINI_API_KEY=your_api_key_here" > .env.local

# 4. 개발 서버 실행
npm run dev

# 5. 브라우저에서 접속
# http://localhost:5173
```

### 9.3 빌드 및 배포

```bash
# 프로덕션 빌드
npm run build

# 빌드 결과 미리보기
npm run preview

# 배포 (Netlify 예시)
# 1. dist 폴더를 Netlify에 드래그 앤 드롭
# 2. 또는 GitHub 연동으로 자동 배포
```

### 9.4 Google API 설정

1. **Google Cloud Console** 접속: https://console.cloud.google.com/

2. **새 프로젝트 생성**

3. **API 활성화**:
   - Google Calendar API
   - Google Tasks API
   - Google Drive API

4. **OAuth 2.0 클라이언트 ID 생성**:
   - 애플리케이션 유형: 웹 애플리케이션
   - 승인된 JavaScript 원본: `http://localhost:5173`, `https://your-domain.com`
   - 승인된 리디렉션 URI: 동일

5. **API 키 생성**:
   - 사용자 인증 정보 → API 키 만들기

6. **앱에 설정**:
   - 설정 메뉴에서 API 키와 클라이언트 ID 입력

---

## 10. API 및 외부 서비스 연동

### 10.1 Google Gemini API

**모델별 용도**:

| 모델 | 용도 | 특징 |
|------|------|------|
| `gemini-2.5-flash` | 음성 전사 | Audio-native, 저지연, 빠른 응답 |
| `gemini-3-pro-preview` | 차트 생성 & 검수 | 높은 정확도, 복잡한 추론 |

**API 호출 예시**:
```typescript
const ai = new GoogleGenAI({ apiKey: geminiApiKey });
const response = await ai.models.generateContent({
    model: 'gemini-2.5-flash',
    contents: {
        parts: [
            { inlineData: { mimeType: 'audio/webm', data: base64Data } },
            { text: promptText }
        ]
    }
});
```

**요금** (2025년 기준):
- Gemini 2.5 Flash: $0.001 / 1K characters (음성 전사)
- Gemini 3 Pro: $0.01 / 1K characters (차트 생성)

### 10.2 Google Calendar API

**사용 기능**:
- `calendar.events.list`: 오늘 일정 조회

**요청 예시**:
```typescript
const response = await window.gapi.client.calendar.events.list({
    'calendarId': 'primary',
    'timeMin': today.toISOString(),
    'timeMax': tomorrow.toISOString(),
    'showDeleted': false,
    'singleEvents': true,
    'orderBy': 'startTime'
});
```

### 10.3 Google Tasks API

**사용 기능**:
- `tasks.tasks.list`: 할 일 목록 조회
- `tasks.tasks.insert`: 새 할 일 추가
- `tasks.tasks.update`: 할 일 수정 (제목, 상태)
- `tasks.tasks.delete`: 할 일 삭제

### 10.4 Google Drive API

**사용 기능**:
- `drive.files.list`: 폴더 검색
- `drive.files.create`: 파일 업로드 (Multipart)

**업로드 코드**:
```typescript
const boundary = '-------314159265358979323846';
const metadata = {
    name: filename,
    mimeType: 'text/plain',
    parents: [folderId]
};

const multipartRequestBody =
    delimiter +
    'Content-Type: application/json; charset=UTF-8\r\n\r\n' +
    JSON.stringify(metadata) +
    delimiter +
    'Content-Type: text/plain; charset=UTF-8\r\n\r\n' +
    soapChart +
    close_delim;

const request = window.gapi.client.request({
    'path': '/upload/drive/v3/files',
    'method': 'POST',
    'params': {'uploadType': 'multipart'},
    'headers': {
        'Content-Type': 'multipart/related; boundary="' + boundary + '"'
    },
    'body': multipartRequestBody
});
```

---

## 11. 보안 및 권한 관리

### 11.1 API 키 관리

**저장 위치**:
- Gemini API 키: `localStorage.getItem('geminiApiKey')`
- Google API 키: `localStorage.getItem('googleApiKey')`
- Google Client ID: `localStorage.getItem('googleClientId')`

**보안 권장사항**:
- ⚠️ **주의**: 현재는 localStorage에 평문 저장
- 🔒 **개선 필요**: 환경 변수 또는 백엔드 서버에서 관리
- 🔐 **향후 계획**: API 키 암호화 또는 서버리스 함수 사용

### 11.2 Google OAuth 스코프

**요청하는 권한**:
```javascript
scope: 'https://www.googleapis.com/auth/calendar.readonly
        https://www.googleapis.com/auth/tasks
        https://www.googleapis.com/auth/drive.file'
```

**권한 설명**:
- `calendar.readonly`: Calendar 읽기 전용
- `tasks`: Tasks 읽기/쓰기
- `drive.file`: 앱이 생성한 파일만 접근

### 11.3 브라우저 권한

**필요 권한**:
- **마이크 접근**: `navigator.mediaDevices.getUserMedia()`
- **클립보드 쓰기**: `navigator.clipboard.writeText()`

**권한 요청 처리**:
```typescript
try {
    const stream = await navigator.mediaDevices.getUserMedia({ audio: true });
    // 녹음 시작
} catch (err) {
    switch (err.name) {
        case 'NotAllowedError':
            setError('마이크 권한이 거부되었습니다.');
            break;
        case 'NotFoundError':
            setError('마이크를 찾을 수 없습니다.');
            break;
    }
}
```

### 11.4 HTTPS 요구사항

**필수 사항**:
- MediaRecorder API는 HTTPS 또는 localhost에서만 작동
- 프로덕션 배포 시 반드시 SSL 인증서 필요

---

## 12. 향후 개선 계획

### 12.1 단기 계획 (1-3개월)

**기능 개선**:
- [ ] 오프라인 모드 지원 (IndexedDB에 임시 저장)
- [ ] 다국어 지원 (영어, 중국어)
- [ ] 차트 템플릿 커스터마이징 기능
- [ ] 환자 이력 관리 기능
- [ ] 통계 대시보드 (일일 진료 수, 주호소 분석)

**UX 개선**:
- [ ] 다크 모드 지원
- [ ] 키보드 단축키 추가
- [ ] 모바일 반응형 디자인 최적화
- [ ] 로딩 상태 개선 (스켈레톤 UI)

**성능 최적화**:
- [ ] React.memo로 불필요한 리렌더링 방지
- [ ] 코드 스플리팅 (React.lazy)
- [ ] 이미지 최적화 (WebP 포맷)

### 12.2 중기 계획 (3-6개월)

**인프라**:
- [ ] 백엔드 API 서버 구축 (Node.js + Express)
- [ ] 데이터베이스 도입 (PostgreSQL)
- [ ] 사용자 인증 시스템 (JWT)
- [ ] API 키 서버 측 관리

**AI 고도화**:
- [ ] 진단명 자동 추천 시스템
- [ ] 치료법 추천 AI
- [ ] 한약 처방 자동 생성
- [ ] 유사 증례 검색

**연동 확대**:
- [ ] 전자의무기록(EMR) 시스템 연동
- [ ] 보험청구 시스템 연동
- [ ] 환자 예약 시스템 연동

### 12.3 장기 계획 (6-12개월)

**사업화**:
- [ ] SaaS 플랫폼 런칭
- [ ] 결제 시스템 도입 (Stripe)
- [ ] 구독 관리 대시보드
- [ ] 관리자 페이지

**고급 기능**:
- [ ] 음성 명령으로 차트 수정
- [ ] 실시간 협업 (여러 의사 동시 작업)
- [ ] AI 챗봇 (환자 증상 사전 문진)
- [ ] 예측 분석 (재방문율, 치료 성공률)

**규제 준수**:
- [ ] 의료정보 보안 표준 준수 (HIPAA)
- [ ] 개인정보보호법 완전 준수
- [ ] 의료기기 인증 검토

---

## 13. 문제 해결 가이드

### 13.1 자주 발생하는 문제

#### 문제 1: 마이크 권한 거부
**증상**: "마이크 사용 권한이 거부되었습니다" 오류

**해결 방법**:
1. 브라우저 주소창 왼쪽 자물쇠 아이콘 클릭
2. "사이트 설정" 클릭
3. "마이크" 권한을 "허용"으로 변경
4. 페이지 새로고침

#### 문제 2: Google API 오류
**증상**: "Google API 초기화에 실패했습니다"

**해결 방법**:
1. 설정에서 Google API 키와 클라이언트 ID 확인
2. Google Cloud Console에서 API 활성화 확인
3. OAuth 승인된 도메인에 현재 URL 추가
4. 브라우저 캐시 삭제 후 재시도

#### 문제 3: 전사 결과가 비어있음
**증상**: 녹음 후 전사 내용이 표시되지 않음

**원인**:
- 마이크가 음소거 상태
- 배경 소음이 너무 큼
- Gemini API 할당량 초과

**해결 방법**:
1. 마이크 음소거 해제
2. 조용한 환경에서 테스트
3. Gemini API 사용량 확인
4. 추가 메모에 텍스트 직접 입력하여 차트 생성 테스트

#### 문제 4: SOAP 차트가 생성되지 않음
**증상**: "SOAP 차트 생성에 실패했습니다" 오류

**해결 방법**:
1. Gemini API 키 유효성 확인
2. 네트워크 연결 확인
3. 브라우저 개발자 도구에서 콘솔 로그 확인
4. API 할당량 초과 여부 확인

### 13.2 디버깅 팁

**브라우저 개발자 도구 활용**:
```javascript
// 콘솔에서 상태 확인
console.log('녹음 상태:', isRecording);
console.log('전사 내용:', transcript);
console.log('SOAP 차트:', soapChart);
```

**네트워크 탭**:
- Gemini API 요청/응답 확인
- Google API 호출 상태 확인
- 오류 코드 분석

**로컬스토리지 확인**:
```javascript
// 콘솔에서 저장된 API 키 확인
localStorage.getItem('geminiApiKey');
localStorage.getItem('googleApiKey');
localStorage.getItem('googleClientId');
```

---

## 14. 유지보수 가이드

### 14.1 정기 점검 항목

**매주**:
- [ ] Google API 사용량 모니터링
- [ ] Gemini API 할당량 확인
- [ ] 에러 로그 검토

**매월**:
- [ ] 의존성 패키지 업데이트 확인
- [ ] 보안 취약점 스캔 (`npm audit`)
- [ ] 사용자 피드백 반영

**분기별**:
- [ ] 백업 데이터 정합성 검증
- [ ] 성능 벤치마크 테스트
- [ ] Google Cloud 비용 최적화

### 14.2 의존성 업데이트

```bash
# 패키지 업데이트 확인
npm outdated

# 보안 취약점 확인
npm audit

# 자동 수정 가능한 취약점 패치
npm audit fix

# 주요 버전 업그레이드 (주의 필요)
npm install react@latest react-dom@latest
npm install typescript@latest
```

### 14.3 백업 전략

**코드 백업**:
- GitHub에 자동 백업 (매 커밋)
- 주요 버전은 태그로 표시

**데이터 백업**:
- Google Drive에 자동 저장
- 로컬 다운로드 권장

**설정 백업**:
- API 키는 안전한 곳에 별도 보관
- 환경 변수 파일 암호화 저장

### 14.4 모니터링 권장사항

**향후 도입 추천**:
- **Sentry**: 에러 추적
- **Google Analytics**: 사용자 행동 분석
- **Lighthouse**: 성능 모니터링
- **Uptime Robot**: 서비스 가용성 모니터링

---

## 15. 참고 자료

### 15.1 공식 문서

- **React**: https://react.dev/
- **TypeScript**: https://www.typescriptlang.org/
- **Vite**: https://vitejs.dev/
- **Tailwind CSS**: https://tailwindcss.com/
- **Google Gemini AI**: https://ai.google.dev/
- **Google Calendar API**: https://developers.google.com/calendar
- **Google Tasks API**: https://developers.google.com/tasks
- **Google Drive API**: https://developers.google.com/drive

### 15.2 관련 표준

- **SOAP 차트 형식**: https://en.wikipedia.org/wiki/SOAP_note
- **의료정보 보안**: HIPAA (미국), 개인정보보호법 (한국)
- **웹 접근성**: WCAG 2.1

### 15.3 커뮤니티

- **GitHub Issues**: https://github.com/choi0ne/b/issues
- **Stack Overflow**: React, TypeScript 관련 질문

---

## 16. 라이센스 및 저작권

**프로젝트 라이센스**: (명시 필요)

**사용 오픈소스**:
- React (MIT License)
- TypeScript (Apache License 2.0)
- Tailwind CSS (MIT License)
- Vite (MIT License)

**상업적 사용**:
- Google Gemini API: 유료 사용 (종량제)
- Google Cloud APIs: 유료 사용

---

## 17. 연락처 및 지원

**프로젝트 관리자**: choi0ne
**GitHub**: https://github.com/choi0ne/b
**이메일**: (명시 필요)

**기술 지원**:
- GitHub Issues를 통한 버그 리포트
- Pull Request 환영

---

## 18. 변경 이력

| 버전 | 날짜 | 변경 내용 |
|------|------|----------|
| 1.0.0 | 2025-12-03 | 최초 정식 버전 배포 |
| 0.9.0 | 2025-12-02 | 베타 버전 - Google 연동 완성 |
| 0.5.0 | 2025-12-01 | 알파 버전 - 기본 기능 완성 |
| 0.1.0 | 2025-11-30 | 프로젝트 시작 |

---

## 19. 용어집

- **SOAP**: Subjective, Objective, Assessment, Plan - 의료 기록 표준 형식
- **청킹 (Chunking)**: 큰 데이터를 작은 조각으로 나누는 기법
- **전사 (Transcription)**: 음성을 텍스트로 변환하는 과정
- **환각 (Hallucination)**: AI가 실제 없는 내용을 생성하는 현상
- **OAuth 2.0**: 안전한 권한 위임을 위한 개방형 표준
- **JWT**: JSON Web Token - 사용자 인증을 위한 토큰 방식
- **API**: Application Programming Interface - 프로그램 간 상호작용 인터페이스
- **SaaS**: Software as a Service - 클라우드 기반 소프트웨어 서비스

---

**문서 작성일**: 2025-12-09
**최종 수정일**: 2025-12-09
**문서 버전**: 1.0.0

---

© 2025 DJD Quality-improvement in Clinical Practice. All rights reserved.
본 문서는 내부 개발 및 유지보수 목적으로 작성되었습니다.
